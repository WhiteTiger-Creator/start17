Act as the regulatory-reporting engineer picking up a submission run that has been stalled since a failed reference-data migration. It truncated the authoritative transaction ledger at `/app/data/transaction_ledger.json` and left the reporting engine at `/app/workflow/build_report.go` deciding what to file under draft rules the compliance committee later reversed. Nothing the engine files can be trusted until that ledger is rebuilt. A pre-migration snapshot and a journal of the changes the migration posted after it survive beside the truncated file under `/app/data`. How the two combine, which booking a change names, what a withdrawal does, what a later reinstatement brings back and the order of the result are governance decisions, and the ledger must be restored at its expected path first.

Then restore the engine, a Go program built with the toolchain installed here and staying a single `package main`. Preserve its `--input` and `--output-dir` options and their defaults; the counterparty register, the regulatory calendar, the rate table and the reporting policy are always read from their fixed absolute paths under `/app/data`, and `--input` selects the ledger only. `/app/docs/reporting_contract.json` is the output contract: paths, schemas, required-field lists, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/compliance_governance_log.md`, mostly routine committee minutes, where the later dated decision governs. A run writes exactly `/app/output/summary.json`, `/app/output/report_lines.json` and `/app/output/exception_queue.jsonl` inside the contract's 90-second budget. Derive every value from the operational inputs: correct against an alternate ledger, identical across reruns, and leave the frozen snapshot `/app/workflow/.build_report.original.go` exactly as it is.
